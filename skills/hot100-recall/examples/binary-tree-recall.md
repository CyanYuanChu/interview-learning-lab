# 二叉树回忆题示例

## 题目

如果 dfs(root) 返回当前子树的最大高度，空节点和当前节点分别返回什么？

## 预期要点

- 空节点返回 0；
- 当前节点返回 max(left, right) + 1；
- 递归结果先完成，再计算当前节点。

## 追问

如果题目要求的是直径，为什么父节点不能直接接收 left + right？

## 评分信号

回答应区分“返回给父节点的单边贡献”和“经过当前节点更新的全局答案”。
