# Interview Learning Lab 演进计划

## 1. 定位与目标

面向技术面试，围绕「理解知识 → 独立作答 → 面试追问 → 弱点复盘 → 再次练习」组织内容和工具。

以现有二叉树、图论与 Trie 专题作为第一批完整样例，逐步扩展算法、岗位知识、系统设计和项目表达。近期示例沿用现有 Java 内容，新增领域按实际面试需求选择。

需要实现的能力：

- 下载仓库后，按 README 即可阅读笔记、打开动画、完成练习。
- 新增专题主要通过添加内容和题库完成，复用已有页面与复盘流程。
- 笔记、参考资料、题库、Skill 各自有明确的维护位置。
- 内容可以用于 Typora、本地浏览器和后续网站，避免维护多份正文。
- 目录、题库和页面升级时，已有链接与个人学习记录有明确的迁移办法。

## 2. 目录设计

保留 `notes/`、`references/`、`skills/`、`apps/`、`docs/` 的主体划分。下图是分阶段目标；新目录随实际内容创建，不提前填充占位文件。

```text
interview-learning-lab/
├── README.md                         # 定位、快速开始、学习入口
├── CONTRIBUTING.md                   # 内容贡献入口与必要规则
├── notes/
│   ├── README.md                     # 领域导航
│   └── algorithms/
│       ├── README.md                 # 算法专题导航
│       ├── trees/
│       │   ├── hot100-binary-tree.md
│       │   └── demos/                # 与二叉树笔记配套的动画
│       └── graphs/
│           ├── hot100-graph-trie.md
│           └── demos/                # 与图论笔记配套的动画
├── references/                       # 项目共用的题单、资料与来源
├── skills/                           # 可复用的 Agent 任务说明
├── apps/
│   └── recall/                       # 第二阶段形成的统一练习入口
│       ├── README.md
│       ├── index.html
│       ├── assets/                   # 练习界面脚本和样式
│       ├── banks/                    # 题库 JSON 与题库索引
│       └── schemas/                  # 题库、作答和复盘数据格式
├── scripts/
│   ├── serve.py                      # 第一阶段：本地静态服务
│   ├── check-content.py              # 第一阶段：内容与资源引用检查
│   └── export-quiz.py                # 第二阶段：导出独立 HTML 试卷
├── docs/
│   ├── usage.md                      # Typora、浏览器、Skill 使用方法
│   ├── learning-path.md              # 面试学习路径
│   ├── repository-guide.md           # 目录边界与内容约定
│   └── validation.md                 # 实际可执行的验证方法
└── tests/                            # 随数据契约、迁移和交互功能添加检查
```

现有测验入口在第二阶段迁移期间继续可用。Java、数据库、系统设计、项目讲述等领域，分别在有内容时添加到 `notes/` 下。

### 2.1 内容归属

| 内容 | 维护位置 | 边界 |
| --- | --- | --- |
| 题解、知识理解、通用易错点 | `notes/<领域>/<专题>/` | 面向学习者，保持可独立阅读 |
| 随文动画、示例图 | 同专题的 `demos/`、`assets/` | 与解释它的笔记一起维护 |
| 独立测验或练习工具 | `apps/<应用>/` | 有独立入口、使用说明和数据约定 |
| 共用题单、外部资料索引 | 根目录 `references/` | 记录来源；正文中的个人推导仍放 notes |
| 某个 Skill 专用参考规则 | 该 Skill 的 `references/` | 按任务需要读取，避免复制共用资料 |
| 题库与评分依据 | `apps/recall/banks/` | 页面、导出器和出题 Skill 共用一份题库 |
| 个人作答与模拟面试记录 | 浏览器存储或用户选择的本地文件 | 默认不纳入公共仓库；通用复盘可整理进 notes |

目录按内容用途划分。即使动画能单独打开，只要主要服务于一篇笔记，就可以保留在该专题中。

### 2.2 导航与文档

- 根 README 负责介绍项目、给出最短使用步骤和内容导航。
- `notes/README.md` 管领域导航，各领域 README 管专题导航。
- 仅在目录有多个入口或独立使用方式时增加局部 README。
- 运行细节集中在 `docs/usage.md`，目录约定集中在 `docs/repository-guide.md`，其他文档引用它们。
- 根 `references/` 优先按主题扩展，来源属性写在文档中；已有来源索引可以继续保留。

## 3. 可扩展性约定

### 3.1 内容、路径与身份

- Markdown 是笔记正文的唯一维护来源，动画源码与笔记同专题存放。
- 对外被题库、复盘或动画引用的章节使用稳定锚点，避免标题改动导致引用失效。
- 专题、题库和题目有稳定 ID，ID 不依赖显示标题、题目排序或文件所在目录。
- 一道题可通过多个标签被检索，但只在一个题库位置维护；其他题单通过 ID 引用。
- 文件移动时同步更新索引、普通链接、iframe 路径和题库来源引用，保留查询参数及锚点。
- 先按完整专题维护笔记；当不同小节需要独立复习或频繁链接时，再按知识单元拆分。

### 3.2 题库与练习界面

第二阶段将题库从页面脚本中提取为 JSON，建立以下最小数据约定：

| 对象 | 必需信息 | 作用 |
| --- | --- | --- |
| 题库 | 格式版本、题库 ID、内容版本、专题 ID、标题、题目列表 | 标识格式与整套题目的内容版本 |
| 题目 | 题目 ID、修订号、题型、题干、知识标签、来源引用、评分依据 | 支撑渲染、评分和回溯到笔记 |
| 作答记录 | 格式版本、记录 ID、题库 ID 与内容版本、逐题修订号与答案、时间 | 记录当时答的是哪一版题目 |
| 复盘结果 | 对应作答记录、逐题评价、错因标签、下一次练习建议 | 区分真实作答与后续评阅 |

具体字段名、必填项和可选项在实现时统一写入 `schemas/`。格式版本用于兼容读取，内容版本用于追踪题意或评分依据的变化。

- 题库索引只维护题库文件入口，标题、标签等信息读取题库本身。
- 新增专题时添加题库和索引记录，不修改页面核心逻辑。
- 通过题型映射组织渲染、答案校验和评分，新增题型只扩展对应处理器。
- 第一批覆盖选择、判断、代码补全、口述解释；题型专属字段由格式校验约束。
- 选择、判断支持确定性评分；代码与口述支持参考要点和人工或 Agent 评阅，未评阅时明确显示「待评阅」。
- 出题 Skill 的结果遵循相同格式，并在导入前验证；不生成另一套与 App 分离的题库标准。
- 网站与后续工具优先读取这份题库。只有出现独立应用共同维护题库的实际需求时，才将其提升为共享内容目录。

### 3.3 学习记录与兼容性

- 学习记录使用独立存储命名空间，按题库 ID、题目 ID 和版本关联。
- 提供可导入的 JSON 备份，以及用于阅读、评分的 Markdown 答案包。
- 导入时检查格式版本、题目映射和重复记录；拒绝不支持的数据时保留当前记录。
- 题意或评分依据变化后，旧结果保留对应版本；需要重新评估的题目标记为待复习。
- 从现有测验迁移时保留 `BT-01` 等题目身份，并提供旧记录到新格式的转换。
- `file://`、本地 HTTP 和线上域名的存储可能相互隔离，跨入口迁移以显式导出、导入为准。
- 旧测验入口先保留读取与导出能力，待迁移验证通过再收敛入口；不自动清空原记录。

### 3.4 本地运行与在线发布

第一阶段统一提供仓库内的 Python 3 静态服务：

- 服务根目录根据脚本所在位置确定，避免依赖启动时的工作目录。
- 默认绑定 `127.0.0.1:8765`，与 Typora 中的嵌入地址保持一致。
- 启动后给出练习页面地址与停止方式；端口占用时明确报错。
- README 给出 macOS、Linux、Windows 的启动方式，不依赖个人机器上的绝对路径或工具安装目录。
- 笔记保留文字说明和普通动画链接，使不支持 iframe 的阅读器仍能阅读核心内容。

第二阶段的统一练习入口通过 HTTP 加载 JSON 题库。需要双击打开的试卷，由导出脚本将题库、脚本和样式打包成独立 HTML，避免依赖 `file://` 下的 JSON 请求。

后续网站直接构建现有 Markdown、动画和题库。构建时将约定的本地 iframe 地址转换为部署路径，保留参数和锚点，并支持部署在子路径下。网站产物与独立试卷输出到 `dist/`，正文和题库只修改源文件。

### 3.5 Skills 与维护工具

- `skills/` 是项目学习技能的唯一源码位置，每项技能写清输入、输出、关联资料和使用示例。
- Agent 集成目录负责接入这些能力，优先使用工具支持的引用或可重复生成方式，避免手工维护多份相同指令。
- 在 `skills/README.md` 提供可直接复制的使用请求，以及已验证的接入方式。
- 新增技能以独立任务为依据，例如模拟面试、项目追问；同一工作流的不同专题优先复用已有技能。
- Spec Kit 属于可选维护工具，保留范围按实际开发流程决定，不作为读笔记、做练习的前置条件。

## 4. 分阶段执行

### 第一阶段：形成可复现的本地使用入口

产出：完整专题目录、本地服务、使用文档和基础检查。

- [ ] 将现有专题动画整理到对应 `demos/`，更新所有笔记和导航引用。
- [x] 增加 `scripts/serve.py`，提供稳定的本地服务入口。
- [x] 补充 `docs/usage.md`，覆盖 Typora、浏览器、端口占用、停止服务和 Skill 使用示例。
- [x] 精简并完善 README 的快速开始；同步调整目录规则与贡献入口。
- [ ] 增加 `scripts/check-content.py`，检查本地文件链接、iframe 资源和必要入口；本地回环 URL 映射为仓库内路径检查，不要求先启动服务。
- [ ] 将内容检查接入轻量 CI，并从准备发布的 Git 文件集合验证所需资源齐全。

验收：

- 在全新下载的目录中，可以按 README 启动服务并打开测验。
- 在 Typora 中实际打开两个专题，确认动画加载及关键交互可用；浏览器加载成功不能替代此项检查。
- 随文动画的播放、单步和重置等现有控件保持可用。
- README 中说明的核心流程没有个人目录或本机 Skill 依赖。

### 第二阶段：建立可复用的练习与复盘能力

依赖第一阶段的运行入口。产出：统一练习 App、两套题库、可迁移的学习记录。

- [ ] 定义题库、作答记录和复盘结果的最小格式及版本规则。
- [ ] 建立 `apps/recall/`，分离题库、页面渲染、评分和存储逻辑。
- [ ] 迁移二叉树测验，增加图论与 Trie 题库，用两个真实专题验证复用方式。
- [ ] 增加确定性题目评分、待评阅状态、错因标签，以及从错题返回笔记的入口。
- [ ] 提供答案包、记录导入导出和旧记录转换，更新 Hot100 Recall Skill 的输出约定。
- [ ] 增加独立 HTML 试卷导出，保持题库只有一个维护来源。
- [ ] 为数据兼容、题型评分和记录迁移增加有针对性的检查，验证最短完整交互流程。

验收：

- 新增一套专题练习只需添加题库和索引，不复制界面或修改通用存储逻辑。
- 可以完成「作答 → 评分或评阅 → 定位弱点 → 回看笔记 → 再练习」。
- 刷新恢复、导出导入和旧测验迁移不会覆盖其他题库的进度。
- 不兼容或损坏的导入文件会产生明确提示，现有数据保持可用。
- 导出的 HTML 可直接在浏览器中使用；经 HTTP 打开的统一 App 使用同一份题库内容。

### 第三阶段：按岗位扩展内容

依赖前两个专题的流程验证。产出：可以持续新增的面试专题。

- [ ] 按实际复习顺序补充数组与哈希、双指针、链表、回溯、动态规划等算法专题。
- [ ] 根据目标岗位选择 Java/JVM、并发、数据库、缓存、网络等知识领域。
- [ ] 增加系统设计、项目讲述或模拟面试内容时，先完成一个实际样例，再抽取复用规则。
- [ ] 每个专题明确知识目标、关键原理、典型问题、边界追问、参考资料和复盘入口；动画按理解难点添加。
- [ ] 更新领域与专题索引，以是否能独立讲清和完成练习记录进展。

验收：新增领域可以复用现有内容导航、题型和复盘格式；确有新题型时，通过扩展对应处理器接入。

### 第四阶段：提供在线阅读与检索

启动条件：多个专题已经稳定，出现持续在线阅读或分享的需求。

- [ ] 选择适合现有 Markdown 和 HTML 动画的静态构建方案，记录必要依赖和构建命令。
- [ ] 从现有源内容生成网站，提供领域导航、专题检索和练习入口。
- [ ] 在构建中处理笔记链接、动画地址和部署前缀，保留本地 Typora 工作流。
- [ ] 增加部署产物的内部链接及关键交互检查，再发布网站。

验收：线上动画与练习无需访问读者本机服务；根路径和配置的子路径下均能找到资源；内容更新只需修改源文件一次。

## 5. 扩展时的决策规则

| 新需求 | 优先扩展位置 | 何时增加新结构 |
| --- | --- | --- |
| 新算法或岗位专题 | notes、参考资料、题库及索引 | 有第一份实际内容时创建专题目录 |
| 新题型 | 题型处理器与数据格式 | 现有题型无法表达作答或评分方式时 |
| 新 Agent 能力 | 现有 Skill 的参数或新 Skill | 工作流、输入输出有实质区别时 |
| 多页面共用动画组件 | 先在同专题或同 App 内复用 | 出现稳定的多个使用方后再提取共享模块 |
| 可运行代码实验 | 与所属专题就近组织 | 形成独立依赖和运行流程后再建立实验模块 |
| 在线展示 | 构建与发布层 | 复用现有内容，按真实访问需求启动 |
| 跨设备学习 | JSON 导入导出 | 只有手动迁移无法满足需求时再评估同步服务 |

实施时优先完成第一、第二阶段。第三阶段按面试需求持续补充内容，第四阶段按使用需求启动。长期使用说明和结构约定归入 README 与 docs，本计划不承载日常学习记录或执行过程日志。
